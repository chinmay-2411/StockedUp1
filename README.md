
  # Build app as specified

  This is a code bundle for Build app as specified. The original project is available at https://www.figma.com/design/hCHPHLDzoHuwH2oBWSb4Mu/Build-app-as-specified.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  